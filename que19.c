
#include <stdio.h>
int main()
{
char enter;
    printf("enter a letter");
        scanf(" %c", &enter);
enter >= 'a' && enter <= 'z'?printf("smaller case letter"):printf("not a smaller case letter");
return 0;
}